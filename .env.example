# ═══════════════════════════════════════════════════════════════
# FRONTEND Environment Configuration
# ═══════════════════════════════════════════════════════════════
# This is the FRONTEND UI that users interact with
# Port: 3001 (default)
# ═══════════════════════════════════════════════════════════════

# ┌─────────────────────────────────────────────────────────────┐
# │ APP URLs (Required)                                         │
# └─────────────────────────────────────────────────────────────┘
# Development:
NEXT_PUBLIC_BASE_URL=http://localhost:3001         # ← Frontend URL
NEXT_PUBLIC_API_URL=http://localhost:3002          # ← Backend API URL (direct)

# Production (direct connection to Railway backend):
# NEXT_PUBLIC_BASE_URL=https://downaria.vercel.app
# NEXT_PUBLIC_API_URL=https://xtfetch-api-production.up.railway.app  # ← Railway backend (direct!)

# ┌─────────────────────────────────────────────────────────────┐
# │ SUPABASE (Required)                                         │
# │ Get from: supabase.com → Project → Settings → API           │
# │ Frontend ONLY needs anon key (NEVER use Service Role Key!)  │
# └─────────────────────────────────────────────────────────────┘
NEXT_PUBLIC_SUPABASE_URL=https://xxxxx.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGci...          # ← Safe for browser

# ┌─────────────────────────────────────────────────────────────┐
# │ PUSH NOTIFICATIONS (Optional)                               │
# │ Frontend ONLY needs public key                              │
# └─────────────────────────────────────────────────────────────┘
NEXT_PUBLIC_VAPID_PUBLIC_KEY=BPxxx...              # ← Safe for browser

# ┌─────────────────────────────────────────────────────────────┐
# │ LOGGING (Optional)                                          │
# └─────────────────────────────────────────────────────────────┘
LOG_LEVEL=info                                     # error | info | debug

# ═══════════════════════════════════════════════════════════════
# IMPORTANT NOTES:
# ═══════════════════════════════════════════════════════════════
# 
# ✅ DO's:
# - Use NEXT_PUBLIC_* prefix for browser-safe variables
# - Only store public keys (VAPID public, Supabase anon key)
# - Store backend API URL
#
# ❌ DON'Ts:
# - NEVER store SUPABASE_SERVICE_ROLE_KEY here
# - NEVER store ENCRYPTION_KEY here
# - NEVER store VAPID_PRIVATE_KEY here
# - NEVER store REDIS credentials here
# - NEVER store JWT_SECRET here
#
# All sensitive keys should be in BACKEND .env only!
# ═══════════════════════════════════════════════════════════════
